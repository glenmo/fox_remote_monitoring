# Fox ESS Monitor — Apache 2 reverse-proxy vhost
# Copies to /etc/apache2/sites-available/fox-monitor.conf
# Enable with:  sudo a2ensite fox-monitor.conf

<VirtualHost *:80>
    ServerName  desky.local
    ServerAlias 192.168.55.33

    # Drop legacy Fronius DataManager UI probes — these aren't us. Blocked
    # at Apache level so they never reach Flask and clutter the journal.
    # If you ever want to find the source again, comment this out and watch
    # the access log: sudo tail -f /var/log/apache2/fox-monitor-access.log
    <LocationMatch "^/(img/Fronius-Logo|uiLib/|src/style/|product/list|point\.shtml|favicon\.ico|device-manager)">
        Require all denied
        ErrorDocument 403 ""
    </LocationMatch>

    # Reverse proxy to the Flask backend bound to 127.0.0.1:5000
    ProxyPreserveHost On
    ProxyRequests     Off
    ProxyPass        / http://127.0.0.1:5000/
    ProxyPassReverse / http://127.0.0.1:5000/

    # Make sure browsers don't cache API responses
    <LocationMatch "^/api/">
        Header set Cache-Control "no-store, no-cache, must-revalidate"
    </LocationMatch>

    ErrorLog  ${APACHE_LOG_DIR}/fox-monitor-error.log
    CustomLog ${APACHE_LOG_DIR}/fox-monitor-access.log combined
</VirtualHost>
